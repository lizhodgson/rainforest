<h1>Products#index</h1>
    <p>Find me in app/views/products/index.html.erb</p>

    <%= link_to "New Book", "/product/new" %>

    <%= @products.each do |product| %>
  <p><%= product.name %>,  <%= product.description %> , <%= product.price_in_cents %></p>
<% end %>





<!-- <h1>Products#index</h1>
<p>Find me in app/views/products/index.html.erb</p>

puts product.formatted_price




<h1>Product</h1>
<%= link_to "New Product", "/products/new" %>

<% @products.each do |product| %>
  <p><%= product.title %>, <%= product.artist %></p>
  <%= link_to image_tag(product.url), "/product/#{picture.id}" %>
<% end %> -->
